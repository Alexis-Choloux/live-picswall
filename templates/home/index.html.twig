{% extends "base.html.twig" %}

{% block body %}
<div {{ stimulus_controller('picswall') }}>
    <input type="text" {{ stimulus_target('picswall', 'name') }}>

    <button {{ stimulus_action('picswall', 'method') }}>
        Greet
    </button>

    <div {{ stimulus_target('picswall', 'output') }}></div>
</div>
{% endblock %}